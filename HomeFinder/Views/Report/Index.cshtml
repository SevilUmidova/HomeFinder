@{
    ViewData["Title"] = "Reports";
    var userRole = Context.Session.GetString("UserRole");
    bool isAdmin = userRole == "Admin";
}

<div class="container py-4">
    <div class="mb-4">
        <h2 class="mb-1">📊 Reports</h2>
        @if (isAdmin)
        {
            <div class="text-muted small mb-2">Administrator reports</div>
        }
        <div class="text-muted">Выбери отчёт и количество строк (top).</div>
    </div>

    <div class="row g-3">
        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h5 class="card-title">🔥 Most viewed apartments</h5>
                    <p class="card-text text-muted">Топ квартир по просмотрам + график.</p>

                    <form method="get" asp-controller="Report" asp-action="MostViewedApartments" class="d-flex gap-2">
                        <input type="number" name="top" class="form-control" value="5" min="1" max="50" />
                        <button class="btn btn-primary">Open</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h5 class="card-title">🏙 Most popular districts</h5>
                    <p class="card-text text-muted">Сумма просмотров по району (district) + график.</p>

                    <form method="get" asp-controller="Report" asp-action="MostViewedDistricts" class="d-flex gap-2">
                        <input type="number" name="top" class="form-control" value="5" min="1" max="50" />
                        <button class="btn btn-primary">Open</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h5 class="card-title">💬 Apartment interactivity</h5>
                    <p class="card-text text-muted">Просмотры vs обращения (appointments), конверсия + сравнения по группам.</p>

                    <form method="get" asp-controller="Report" asp-action="ApartmentInteractivity" class="d-flex gap-2">
                        <input type="number" name="top" class="form-control" value="20" min="1" max="50" />
                        <button class="btn btn-primary">Open</button>
                    </form>
                </div>
            </div>
        </div>

        @if (!isAdmin)
        {
            <div class="col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="card-title">💰 Price analytics</h5>
                        <p class="card-text text-muted">Средняя цена по районам и тренды по времени.</p>

                        <form method="get" asp-controller="Report" asp-action="ApartmentPriceAnalytics" class="d-flex gap-2">
                            <button class="btn btn-primary">Open</button>
                        </form>
                    </div>
                </div>
            </div>
        }

        @if (isAdmin)
        {
            <div class="col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="card-title">👥 Самые активные арендаторы</h5>
                        <p class="card-text text-muted">Топ арендаторов по количеству входов в аккаунт.</p>

                        <form method="get" asp-controller="Report" asp-action="MostActiveTenants" class="d-flex gap-2">
                            <input type="number" name="top" class="form-control" value="20" min="1" max="50" />
                            <button class="btn btn-primary">Open</button>
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
